name: HT Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Messages
        run: |
          echo "Welcome"
          echo "Learning GitHub Actions"
          pwd
          ls